# Nome da aplicação
spring.application.name=PROJETOFINALBACK

# Configuração do Locale
spring.web.locale-resolver=fixed
spring.web.locale=pt_BR

# Configuração do servidor (porta e endereço)
server.port=8080
server.address=0.0.0.0

# Configuração do banco de dados PostgreSQL
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://ep-round-hat-a5d24g0w.us-east-2.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner  # Valor direto
spring.datasource.password=uLK1rd7VeJnY  # Valor direto

# Configuração do JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=update

# Configuração de logs
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

